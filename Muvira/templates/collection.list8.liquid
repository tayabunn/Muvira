
{% if settings.collection_sidebar_use == "left_sidebar" or  settings.collection_sidebar_use == "right_sidebar" %}
{% assign mid_width = "grid__item wide--four-fifths post-large--four-fifths large--four-fifths" %}
{% else %}
{% assign mid_width = "grid__item" %}
{% endif %}
<div class="grid__item">
  <div class="collection-products">
   {% if settings.collection_sidebar_use == "left_sidebar" %}    
    <div class="grid__item wide--one-fifth post-large--one-fifth large--one-fifth left-sidebar">
      {% include 'collection-sidebar' %}
    </div>
    {% endif %}
   <div class="{{ mid_width }} sidebar-hidden"> 
      {% if settings.collection_sidebar_use == "full_width" %}
      {% include 'collection-sidebar' %}
      {% endif %}
      <div class="collection-list">
        <div class="grid-uniform grid-link__container col-main">  
          {% paginate collection.products by 8 %}    
          {% include 'tool-list' %}
          <div class="product-list-view">
            {% if collection.products.size > 0 %}  
            <ul>
              {% for product in collection.products %}
              {% include 'product-list-item' %}
              {% endfor %}
            </ul>
            {% endif %}

            {% if shop.products_count == 0 %}
            <div class="grid__item">
              <p><em>{{ 'home_page.onboarding.no_products_html' | t }}</em></p>
            </div>
            {% endif %}
          </div>
          {% if paginate.pages > 1 %}
          <div class="text-center padding">
            {% include 'pagination-custom' %}
          </div>
          {% endif %}
          {% endpaginate %}  
        </div>
      </div>
    </div>
    {% if settings.collection_sidebar_use == "right_sidebar" %}    
    <div class="grid__item wide--one-fifth post-large--one-fifth large--one-fifth left-sidebar">
      {% include 'collection-sidebar' %}
    </div>
    {% endif %}
   
</div>
</div>


